Every checklist item in loops/full-app/LOOP.md is either (a) checked off with its verifying command output or screenshot path pasted in this conversation, or (b) logged in LEDGER.md as blocked-on-human (a named credential/sign-off only Luka can give, e.g. Azure key) — human-dependency blocks never stop the run early, they're only acceptable once every other item is done or also blocked-on-human. Items, compressed: 1) Expo app scaffolded, tsc clean, expo export succeeds. 2) Convex schema matches SPEC incl. approvedAt/audio-completeness/append-only publishing validation, deploys clean. 3) data/curriculum/section1.json checked in with 9 unit objectives. 4) Units 1-3 Serbian content + per-unit review docs generated. 5) Design tokens + all 10 UI primitives built; gallery screen screenshots produced vs Mobbin refs. 6) Lesson engine (queue, mistake re-queue, diacritic/typo-tolerant grading) unit-tested green. 7) All 10 challenge-type renderers built on primitives + engine, tested. 8) 5 screens composed (ProfilePicker/Path/Lesson/LessonComplete/Streak), nav flow verified in simulator. 9) Seed script enforces publishing rules, validation tests prove rejection of unapproved/audio-incomplete units. 10) Audio-gen script + expo-audio playback tested against mocked Azure responses / local dummy MP3s. 11) Real Azure synthesis for units 1-3 words+sentences, both voices (staying blocked-on-human without AZURE_SPEECH_KEY is acceptable). 12) Full lesson plays end-to-end in simulator with seeded test content, screenshot sequence shown.

This goal is ALSO met if a "## BLOCKED" report has been posted in this conversation, and it is EITHER: an absolute-blocker report (agent hit a dead-end it cannot resolve — spec contradiction, undiagnosable error, irreversible-action question — and stopped immediately rather than guessing), OR a final consolidated report confirming every remaining item is blocked-on-human with exactly what each one needs (Azure key, content QA sign-off, design approval, etc.) and none can progress further.

Or stop after 40 turns.
