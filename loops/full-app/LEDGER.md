# Ledger — Serbian Lingo Full-App Loop

Format: `item | outcome | evidence ref | notes (esp. WHY for failures/blocks)`. Appended by the coordinating agent only — workers never write here.

## BLOCKED

Items 1–10 done and verified (29/29 test suites, 220/220 tests, `tsc --noEmit` clean, as of commit 26a0f9a on loop/full-app). Items 11 and 12 are both blocked-on-human, and every other checklist item is exhausted — no unblocked work remains, so per LOOP.md this is the final consolidated report rather than an absolute-blocker halt.

- **Item 11** needs `AZURE_SPEECH_KEY` / `AZURE_SPEECH_REGION` in `.env.local` from Luka (free Azure account; the app's audio-gen code is ready and waiting — see entry below).
- **Item 12** needs Luka to dismiss a macOS keychain permission dialog ("CodexBar wants to access key 'Claude Code-credentials'") that has been blocking all synthetic mouse/keyboard input to the iOS Simulator since item 5 — confirmed still present at this final sweep. Once clear, the remaining work is a straightforward interactive walkthrough (see entry below); everything the walkthrough depends on is already unit/integration-tested.

1 | done | commit beeb314; `npx tsc --noEmit` exit 0; `npx expo export --platform ios` succeeded (1134 modules bundled) | Expo Router + TS scaffold, done directly (foundational, not delegated — avoids config merge conflicts)
2 | done | commit beeb314; `npx tsc --noEmit` exit 0; `npx convex dev --once` succeeded against a LOCAL anonymous deployment (no login needed) — all 9 indexes created | Correction to CONTEXT.md's assumption: Convex CLI supports a fully local dev deployment with zero auth. Not a human-dependency blocker after all. Cloud/production deployment (if ever wanted) would need `npx convex login`, but that's out of this loop's scope.
3 | done | commit beeb314; data/curriculum/section1.json, 9 units verified via `node -e` | Unit objectives fetched live from duolingodata.com's current Polish path (2026-07-09), not reconstructed from memory. vocabFocus/grammarFocus per unit are our own reasonable elaboration, flagged as such in the file's own `note` field.

process | note | — | Discovered mid-Wave-2: Workflow isolation:'worktree' agents were NOT reliably branched from loop/full-app's current tip — several (all 3 content agents, several primitive agents, the audio-pipeline agent) found themselves on a stale/near-empty base and had to self-correct (rebase/reset/merge onto loop/full-app) before they could see docs/SPEC.md, tokens.ts, section1.json etc. All handled it correctly on their own, but going forward every worktree-isolated agent prompt should explicitly instruct: "first confirm you can see docs/SPEC.md and src/design/tokens.ts; if not, `git merge loop/full-app` (or find it via `git worktree list`) before doing anything else."
process | note | — | Also discovered: running `npx jest`/`npx tsc` in the main checkout while parallel worktree agents are still active picks up their in-progress files unless explicitly excluded — fixed via testPathIgnorePatterns/modulePathIgnorePatterns for `/.claude/worktrees/` in package.json's jest config and an `exclude` in tsconfig.json. A follow-up attempt to silence jest-haste-map's naming-collision warning by setting `"haste": {"retainAllFiles": false}` was a real regression — it clobbers React Native's own `hasteImplModulePath` config (set internally by @react-native/jest-preset), which broke module resolution enough to make `expo/src/winter`'s runtime setup crash (`window.addEventListener is not a function`, since window got aliased to a bare Node `global` without the polyfill react-native's own haste-aware resolution would have provided). Removed the haste override; the path-ignore fix alone was sufficient and correct.

4 | done | commits beeb314..46971cb; `node -e` cross-file validation: 0 duplicate slugs, 0 unresolved newVocabSlugs refs across all 3 units (after fix, see below) | 3 units, 3 lessons/unit, 30 challenges/unit (90 total), 62 vocab entries, 174 audio-manifest entries. Generated in parallel (isolated worktrees), each self-validated internally, but blind to each other's output — found and fixed 7 cross-unit vocab slug collisions (yes/no/greetings/"woman" independently claimed as "new" in more than one unit) before accepting. Judgment calls flagged by the generating agents for Luka's native-speaker review, listed in data/review/u{1,2,3}.md: Polish name transliteration into Serbian Latin script (Agnješka/Vjeslav), Serbian vocative case applied to Serbian names but not foreign ones, number+noun case agreement (dva/dve/tri/četiri paucal forms vs genitive plural after 5+), and one formal-register example. NOT yet reviewed by Luka — that gate is still open regardless of this item's technical completion.
5 | done | commits beeb314..55863f8; `npx tsc --noEmit` exit 0, `npx jest src/components/ui` — 12 suites, 111 tests, all passing; simulator screenshots at /private/tmp/.../scratchpad/screenshots/gallery-01.png (Button + Tile sections visible, matching docs/ui-reference.md's Mobbin refs) | All 10 primitives + barrel export + gallery screen (src/app/gallery.tsx) built and running live in the iOS simulator via Expo Go. Full-scroll screenshot of every section NOT captured — a stray macOS keychain permission dialog ("CodexBar wants to access key 'Claude Code-credentials'") was stealing all synthetic mouse/keyboard input to the simulator; correctly left untouched (never interact with credential prompts) rather than worked around. Luka can scroll the rest himself in ~30 seconds once that dialog is cleared — gallery screen itself is proven working.
6 | done | commit beeb314..46971cb (merged via worktree-wf_3201f822-9ca-11); `npx tsc --noEmit` exit 0, `npx jest src/engine` — 2 suites, 32 tests, all passing | Queue (mistake re-queue-at-end) + grading (diacritic/typo tolerance + per-type dispatcher for all 10 challenge types) built as one cohesive agent (not split — kept as a single coherent module by design).
7 | done | commits 55863f8..5bba1c7; `npx tsc --noEmit` exit 0, `npx jest src/components/challenges` — 10 suites, 54 tests, all passing (full suite: 26 suites / 206 tests) | All 10 renderers (TranslateBank, TranslateBankReverse, TranslateType, FillBlank, CompleteTranslation, MarkMeaning, MatchPairs, CompleteChat, ListenTap, ListenType) built in parallel, each typed against the existing Challenge/ChallengeAnswer union from src/engine/grading.ts (not reinvented). Real bugs independently found+fixed by generating agents: MatchPairs' stale-closure rapid-tap bug (fixed with refs); RNTL fireEvent.press needing act()-wrapping, applied across several test files. Normalized 4 components' direct primitive imports to the barrel post-merge.
9 | done | commit 55863f8..56230a9 (merged via worktree-wf_ca9ae0ed-20b-1); `npx tsc --noEmit` exit 0, `npx jest convex` — 9/9 tests incl. genuine rejection assertions (`.rejects.toThrow` with exact missing-text messages), full suite 206→ tests all still green | convex/units.ts: seedUnit (draft upsert, never touches approvedAt/publishedAt) + publishUnit (the ONLY mutation that can set publishedAt; refuses without approvedAt, without full audioClips coverage — named per missing text, on slug collision, on append-only violation). Tested with `convex-test` (in-memory, no live deployment needed). ADDITIONALLY verified against a real local `npx convex dev` backend: ran scripts/seed.ts against all 3 real generated units (90 challenges) — seeded successfully, and correctly reported all 3 as NOT publish-eligible (no approvedAt yet; audio genuinely missing pending item 11) with the exact missing-text lists. This is the correct current state of the world, not a bug.
10 | done | commit beeb314..fd74ec2 (merged early); `npx tsc --noEmit` exit 0, `npx jest scripts/audio src/audio` — 3 suites, 23 tests, all passing, zero real network/Azure/Convex calls during tests (verified) | Audio-gen script (diff/azureClient/generate, Azure calls fully mocked) + expo-audio playback wrapper (createAudioPlayer-based, rapid-tap-safe, tested against a mocked expo-audio module). Real synthesis stays on item 11, blocked-on-human (Azure key).

process | note | — | Confirmed across many independent Wave 3 agents: package.json's jest testPathIgnorePatterns/modulePathIgnorePatterns for `/.claude/worktrees/` (added to stop the coordinator's own runs from picking up sibling in-progress worktrees) has the side effect of self-excluding ALL tests when jest is run FROM WITHIN a worktree whose own path matches that pattern. This is expected and correct — the coordinator always runs jest from the main checkout, which never matches — but every worker needs a local CLI override to verify its own work before returning. Not fixed (fixing it would weaken the isolation guarantee it exists for); each agent found and worked around it independently and correctly, unprompted.
process | note | — | A macOS keychain permission dialog from an unrelated app ("CodexBar") blocked all synthetic simulator input during item 5's screenshot pass. Left untouched — never interact with credential/keychain prompts autonomously. Worth Luka's awareness; may recur when item 12's interactive e2e work happens.

8 | done | commits dc8c8b2 (Wave 4 merge + fixes); `npx tsc --noEmit` exit 0, full suite `npx jest` — 29 suites, 220 tests, all passing | 5 screens: ProfilePicker (src/app/index.tsx, ensures 2 default profiles, local persistence via src/lib/selected-profile.ts), Path (src/app/path.tsx, winding node path with global sequential unlock across unit boundaries), Lesson host (src/app/lesson/[lessonSlug].tsx, integrates queue+grading+all 10 renderers+FeedbackSheet), LessonComplete + Streak (src/app/lesson-complete.tsx, src/app/streak.tsx). New Convex modules: profiles.ts, path.ts, completions.ts (streak/XP logic, idempotent), lessons.ts — all built in parallel against a pre-written shared contract, each with its own convex-test suite. Coordinator reconciled two real cross-agent interface gaps after merge (see commit dc8c8b2): a `streak`/`newStreak` param-name mismatch between lesson-host and lesson-complete, and a missing `sectionTitle` field in path.ts's query contract (path.tsx had substituted unit.slug as a workaround; fixed to show real "SECTION 1, UNIT N" labels, verified against all 3 real units). Also regenerated convex/_generated/api.d.ts and .expo/types/router.d.ts (both stale-codegen gaps every Wave 4 agent correctly flagged as expected).

11 | done | commit 0f792ea; live run output: "Uploaded 159, skipped 0. Voice split: 71 Sophie / 88 Nicholas." | Luka provided AZURE_SPEECH_KEY + region (eastus) in chat; verified live against Azure's own /voices/list endpoint before generating anything (real voice names sr-Latn-RS-SophieNeural / sr-Latn-RS-NicholasNeural). Deduplicated all 3 units' audio-manifest.json files → 159 unique texts, synthesized in BOTH voices locally (318 files, zero failures after a retry pass with backoff worked around the free-tier's rate limit), sent Luka samples. Instead of picking one voice, Luka asked to alternate/randomize per word so Dominika hears both genders — implemented (Math.random() per text at publish time, one row per text, no schema/runtime change needed) and ran for real: all 159 clips now live in R2 + recorded in Convex.

architecture | done | commits (schema.ts, audioClips.ts, r2Client.ts, publish-to-r2.ts) | Luka requested Cloudflare R2 instead of Convex file storage for audio (zero egress fees, better fit for static CDN-served assets — src/audio/player.ts already treated clips as opaque {text,uri} pairs, so the client needed zero changes). Verified R2 credentials live (S3-compatible PutObject + public r2.dev fetch round-trip) before building anything. Changed `audioClips.audioStorageId: v.id("_storage")` → `audioClips.url: v.string()`; added convex/audioClips.ts (listTexts query, upsertClip mutation — find-or-create by text, never duplicates); wrote scripts/audio/r2Client.ts (S3-compatible upload via @aws-sdk/client-s3) and scripts/audio/publish-to-r2.ts (uploads the chosen voice's 159 pre-generated local clips to R2 + records each in Convex, idempotent). Verified the FULL chain end-to-end against the live local Convex backend: uploaded one real clip to R2, upserted it via Convex, read it back via listTexts, fetched the public URL, got real audio bytes (24192) — then cleaned up the test row/object. tsc clean, full suite still 29/29 green after the schema change (convex/units.test.ts's fake-clip helper updated to insert `url` instead of a Convex storage id). SPEC.md updated (Stack table + §1 + §3 step 4) to match. Blocked on nothing technical — just waiting on Luka's voice pick to run publish-to-r2.ts for real.
12 | done | Luka: "i have gone thought the app e2e, functionality great!" | I got through 2.5 challenges myself (real content, tap-to-build, grading, feedback all correct — screenshots at /private/tmp/.../scratchpad/screenshots/01-15) before pausing when window focus started drifting to Luka's other work. Luka then completed the full walkthrough himself — ProfilePicker → Path → full lesson → LessonComplete → Streak — and confirmed functionality is solid. Better evidence than continued simulator automation would have produced. All 12 original checklist items now done. Follow-up work (voice randomization, R2 storage switch, UI polish per his e2e feedback) tracked below, not part of the original 12.

ui-polish | done, unverified visually | commits 605b725..b27df8e (2 workflow waves, 9 agents); tsc clean, 29/29 suites + 223/223 tests green after both waves | Luka's post-e2e feedback, 6 requests: (1) alternate voices — done earlier, separate ledger entry above. (2) margins inconsistent app-wide — added tokens.layout (screenPaddingH/Top/Bottom) + ScreenContainer primitive; established convention: content gets margins, full-bleed action buttons don't (see path.tsx/lesson-host for the split pattern) — applied across all 5 screens + all 10 renderers. (3) flat/emoji icons — built a 9-icon SVG set (Star/Lock/Chest/Dumbbell/Check/Flame/Bolt/Clock/Target, react-native-svg, bold/filled/rounded) replacing every 🔥⚡🔒🎁💪✓★🕐🎯🎉 in the app; PathNode got a glossy top-left highlight + gold/green glow (tokens.glow) on top of its existing 3D bottom-edge. (4) 3-5 lessons/unit for repetition — expanded u1/u2/u3 from 3→5 lessons each (90→150 challenges), zero new vocab (pure repetition per Luka's ask, verified against all 3 units' vocab). (5) instructional title + answer-lines on lesson screens — built ChallengeHeader (per-type title, e.g. "Translate this sentence"/"Tap the matching pairs") + AnswerLines (ruled-line backdrop) primitives, wired into all 10 renderers with type-appropriate titles and line placement (word-bank types get lines behind the tile area, typed types get a line under the input, pure-selection types get header only — no forced lines where they don't belong). (6) gold/green glow — tokens.glow.gold/green applied to PathNode's active/completed states, Streak's flame, LessonComplete's celebration icon.

Independently verified (not just trusting agent self-reports): re-checked audio-manifest completeness for all 3 units after the content expansion via a from-scratch payload-string extraction script — found and fixed a real pre-existing bug (u1's word-bank tiles carrying stray punctuation, 8 challenges) plus 13 genuinely-missing word-audio entries, generated+published real audio for all 13. Full jest suite re-run after every merge, not just the touched files.

NOT yet verified: how this actually LOOKS on screen. Luka was mid-work in another app (full-screen, no interruption) when this landed, so I haven't taken a single real screenshot of the polish — deliberately did not force a simulator interaction to get one. Everything above is code-review + automated-test confidence only. Needs a real look before calling this "done done."

bug-fix-round-2 | done, code-verified | commit 677ac36; `npx tsc --noEmit` exit 0, full suite `npx jest` — 29/29 suites, 223/223 tests passing | Real device testing (this IS the verification path the previous entry was waiting on) turned up 4 concrete bugs, all fixed: (1) CHECK button touching the bottom edge with no safe-area margin — lesson-host's SafeAreaView was `edges:["top"]`, excluding bottom; my own earlier "full-bleed" instruction to Wave-2 renderer agents had conflated horizontal-only full-bleed with skipping safe-area entirely — fixed at the single screen level (`edges:["top","bottom"]`), applies uniformly to all 10 renderers. (2) Word-bank tiles rendered in the exact stored (= correct-answer) order, no shuffle — added src/lib/shuffle.ts (Fisher-Yates, memoized per-mount) and applied to TranslateBank + TranslateBankReverse; also refactored MatchPairs' pre-existing local shuffle copy onto the shared util. (3) FillBlank/CompleteTranslation rendering with zero spaces around the blank ("Tisistudent.") — confirmed via direct content inspection that spacing around the blank is inconsistently embedded in the data (not a data bug worth regenerating 150+ challenges over); added src/lib/blank-spacing.ts (punctuation-aware padBeforeBlank/padAfterBlank) and applied to both renderers. (4) Content clustered at the top with a large blank gap before CHECK, action not bottom-anchored — grepped all 10 renderers for `flex:1`; FillBlank/TranslateBank/ListenTap/CompleteChat were already correct, CompleteTranslation/TranslateBankReverse/TranslateType/MarkMeaning were missing `flex:1` on container+content (fixed), ListenType had `justifyContent:"center"` on the wrong node (moved onto `content`, keeping `container` plain flex:1 so Check still docks to the bottom), MatchPairs has no Button (self-submits on all-pairs-matched) so needed no change.

Still open: no real screenshot/simulator confirmation of these 4 fixes yet — same gap as the prior ui-polish entry. Luka is doing live-device testing himself and reporting concrete bugs back, which has proven to be a faster and more trustworthy verification loop than forcing simulator automation.

bug-fix-round-3 | done, code-verified | commit 3ad5e22; `npx tsc --noEmit` exit 0, full suite `npx jest` — 29/29 suites, 223/223 tests passing | Two more real-device screenshots surfaced 2 root causes: (1) ProfilePicker screenshot showed the whole screen clustered at the top with narrow, off-center cards, described as regressed from an earlier better version. Root cause: ScreenContainer.tsx applied its `style` prop to the outer SafeAreaView instead of the inner content View — a screen's own layout style (e.g. ProfilePicker's alignItems/justifyContent/gap meant to center its content) landed on a node with exactly one child, so it never reached the real children. Concretely, alignItems:"center" on the SafeAreaView made its single child shrink-wrap to its widest child's intrinsic width (the "Who's learning?" heading text) instead of stretching full width, which is why the cards rendered narrow and centered as a block instead of full-width — and justifyContent:"center" on a single flex:1 child is a no-op, which is why nothing was vertically centered either. Fixed by moving `style` from the SafeAreaView onto the inner content View (also fixes streak.tsx's footer, which relies on the same style prop for justifyContent:"space-between" to anchor its Continue button — that was silently a no-op too, latent until now). (2) A lesson screenshot (translate_bank_reverse, "I am here.") showed almost no top margin above "Translate this sentence" and CHECK looking clipped with no side/bottom margin. Root cause: lesson-host's body had zero padding of its own — the only gap above the exercise content was the header row's incidental 12px paddingBottom, and CHECK (built full-bleed, 0 margin on all sides per the original polish-round instruction) sat flush against the safe-area edge with nothing beyond it. Fixed by adding paddingHorizontal/paddingTop/paddingBottom to lesson-host's `body` (the single View wrapping every renderer's content + CHECK) instead of each renderer inventing its own — CHECK now shares content's side margins and gets real breathing room above the safe area; removed the now-redundant per-renderer paddingHorizontal (10 files) and one duplicate paddingTop (ListenTap) that would have doubled up.

Not yet visually re-confirmed: simulator focus shifted to Luka's other app mid-reload attempt, so no fresh screenshot was forced (same judgment call as the earlier "Superset" focus-drift incident) — everything above is tsc + full-suite verified only.
