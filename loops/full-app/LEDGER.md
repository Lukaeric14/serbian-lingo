# Ledger — Serbian Lingo Full-App Loop

Format: `item | outcome | evidence ref | notes (esp. WHY for failures/blocks)`. Appended by the coordinating agent only — workers never write here.

1 | done | commit beeb314; `npx tsc --noEmit` exit 0; `npx expo export --platform ios` succeeded (1134 modules bundled) | Expo Router + TS scaffold, done directly (foundational, not delegated — avoids config merge conflicts)
2 | done | commit beeb314; `npx tsc --noEmit` exit 0; `npx convex dev --once` succeeded against a LOCAL anonymous deployment (no login needed) — all 9 indexes created | Correction to CONTEXT.md's assumption: Convex CLI supports a fully local dev deployment with zero auth. Not a human-dependency blocker after all. Cloud/production deployment (if ever wanted) would need `npx convex login`, but that's out of this loop's scope.
3 | done | commit beeb314; data/curriculum/section1.json, 9 units verified via `node -e` | Unit objectives fetched live from duolingodata.com's current Polish path (2026-07-09), not reconstructed from memory. vocabFocus/grammarFocus per unit are our own reasonable elaboration, flagged as such in the file's own `note` field.

process | note | — | Discovered mid-Wave-2: Workflow isolation:'worktree' agents were NOT reliably branched from loop/full-app's current tip — several (all 3 content agents, several primitive agents, the audio-pipeline agent) found themselves on a stale/near-empty base and had to self-correct (rebase/reset/merge onto loop/full-app) before they could see docs/SPEC.md, tokens.ts, section1.json etc. All handled it correctly on their own, but going forward every worktree-isolated agent prompt should explicitly instruct: "first confirm you can see docs/SPEC.md and src/design/tokens.ts; if not, `git merge loop/full-app` (or find it via `git worktree list`) before doing anything else."
process | note | — | Also discovered: running `npx jest`/`npx tsc` in the main checkout while parallel worktree agents are still active picks up their in-progress files unless explicitly excluded — fixed via testPathIgnorePatterns/modulePathIgnorePatterns for `/.claude/worktrees/` in package.json's jest config and an `exclude` in tsconfig.json. A follow-up attempt to silence jest-haste-map's naming-collision warning by setting `"haste": {"retainAllFiles": false}` was a real regression — it clobbers React Native's own `hasteImplModulePath` config (set internally by @react-native/jest-preset), which broke module resolution enough to make `expo/src/winter`'s runtime setup crash (`window.addEventListener is not a function`, since window got aliased to a bare Node `global` without the polyfill react-native's own haste-aware resolution would have provided). Removed the haste override; the path-ignore fix alone was sufficient and correct.
